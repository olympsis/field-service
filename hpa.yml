apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: olympsis-field-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: olympsis-field
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80